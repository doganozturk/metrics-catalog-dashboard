import * as React from "react";
import "./Header.css";

const Header: React.FC = () => (
    <div className="Header">
        <div className="logo">
            <span>METRICS CATALOG - DASHBOARD</span>
        </div>
    </div>
);

export default Header;
